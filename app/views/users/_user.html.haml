.row
  .col-md-2
    = image_tag user.authentication.image, class: "img-circle", style:"width:120px"
  .col-md-10
    %h3
      = user.name
    %h5
      = "投稿数 #{user.photos.count}"

%hr
.row
  / TODO: masonryにする
  - user.photos.each do |photo|
    = link_to image_tag(photo.image.image.pickup.url, style: "width:200px"), photo_path(photo)
