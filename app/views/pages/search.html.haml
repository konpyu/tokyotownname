%h2
  まちを探す

.row
  .col-md-5
    %form
      .form-group
        %input.form-control#searchtown(type="text" placeholder="町名を検索")

#search-links
  - @wards.each do |ward|
    .search-links-ward-unit
      %h3.search-ward-name
        = link_to ward.name, ward_path(ward)
      - ward.towns.each do |town|
        %span.search-links-hoge(data-town-name="#{town.name}")
          = link_to town.name, town_path(town)

:javascript
  new Search();
